.list
  .list__header
    %p.list__header--logo タグ一覧
    =icon('fas','cog', class: 'list__header--icon')
  .list__box
    -# - @tags.each do |tag|
    -#   = link_to tag, root_path(tag_list: item.tag_list), class: "link"